import { Section, SectionTitle } from '../../styles/GlobalComponentsStyles'


const Hero = () => {
    return (  
        <Section>
            <SectionTitle>Blog</SectionTitle>            
        </Section>
    );
}
 
export default Hero;